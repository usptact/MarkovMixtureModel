# MarkovMixtureModel
A sample Markov Mixture Model implementation in Infer.NET (C#)

The model accepts a number of sequences with discrete states and clusters them. Each sequence in a cluster is modeled using initial state probabilities and a transition probability matrix.

(Work in progress)
